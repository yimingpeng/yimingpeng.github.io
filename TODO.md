# Portfolio TODO

Quick notes for future improvements that should not show on the live site until they are ready.

## Writing

- Review and publish the first `Portfolio as Product` article from the private Obsidian project.
- Import each live portfolio article into Medium and verify its canonical link.
- Consider tag pages, search, pagination, and generated social images after enough articles exist to
  justify them.

## Projects

- Fill in `writeup` on each entry in `src/data/projects.ts` once the Obsidian blog branch lands —
  currently unset, so cards and archive rows show a muted "deep dive coming" note instead of a
  broken link.

## Content

- Keep the CV update month in sync with the actual PDF.
- Review the certification list after each new Credly badge is earned.
- Add KCNA badge details once the certificate is completed.

## Design

- Revisit whether the certification list should move into a reusable data file and component.
- Consider adding a compact "last updated" note if the homepage starts changing frequently.
